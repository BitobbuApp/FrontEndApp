// Re-export from canonical location (features/auth).
// Kept for backward compatibility so other files that import from here still work.
export { AuthProvider, useAuth } from '@/features/auth/AuthContext';
